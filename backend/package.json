{
  "name": "ai-resume-analyzer-backend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Proxies Gemini (primary) and Groq (fallback) API calls so no API key ever reaches the browser.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "@google/genai": "^2.19.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}